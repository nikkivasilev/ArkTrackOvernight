"""Phase-1 stub. Pipeline gates construction with ``cfg.opencv_hog_enabled`` (False)."""
from __future__ import annotations


class HogDetector:
    def __init__(self, *args, **kwargs):
        raise RuntimeError("HogDetector stubbed in Phase 1; set cfg.opencv_hog_enabled=False")
